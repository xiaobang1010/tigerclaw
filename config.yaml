# TigerClaw 配置文件

gateway:
  host: "0.0.0.0"
  port: 3000
  bind: "loopback"

model:
  default_model: "glm-5"
  providers:
    custom:
      base_url: "https://aigw-gzgy2.cucloud.cn:8443/v1"
      api_key: "${CUSTOM_API_KEY}"
      models:
        - "glm-5"
